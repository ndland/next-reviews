import { Roboto_Slab } from 'next/font/google';

export const RobotoSlab = Roboto_Slab({
  subsets: ['latin'],
});

export const RobotoSlabHeading = Roboto_Slab({ weight: '700', subsets: ['latin'] });
